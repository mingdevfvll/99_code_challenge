/** Switcheo interview API host (no trailing slash). */
export const INTERVIEW_SWITCHEO_ORIGIN = "https://interview.switcheo.com";

/** Relative resource path for the token prices JSON feed. */
export const PRICES_JSON_PATH = "prices.json";

export const PRICES_URL = `${INTERVIEW_SWITCHEO_ORIGIN}/${PRICES_JSON_PATH}`;
